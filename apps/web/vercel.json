{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run db:generate && npm run build --workspace=web",
  "crons": [
    {
      "path": "/api/cron/trigger-sync",
      "schedule": "0 18 * * *"
    }
  ]
}
